<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" x:Name="userControl" x:Class="Zunzun.App.Views.Xaml.Search" mc:Ignorable="d">
    <Grid>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition/>
    		<ColumnDefinition Width="Auto"/>
    	</Grid.ColumnDefinitions>
    	<TextBox x:Name="SearchTXT" SpellCheck.IsEnabled="True" Margin="3,0,0,0" BorderBrush="{DynamicResource InfoBrush}" Background="{DynamicResource ContentBrush}" FontSize="{DynamicResource ContentFontSize}" />
    	<Button x:Name="SearchBTT" Content="S" Style="{DynamicResource DefaultButtonStyle}" Width="18" Height="18" Background="{DynamicResource BTTBrush}" BorderBrush="{DynamicResource InfoBrush}" Foreground="{DynamicResource ContentBrush}" FontSize="{DynamicResource ContentFontSize}" IsDefault="True" Grid.Column="1" d:LayoutOverrides="GridBox" Click="OnSearch"/>
            
    </Grid>
</UserControl>
